# Delve

A multiplayer web app for running Shadowdark RPG campaigns.

**This is attempt two — a fresh start (2026-08-03).**

Attempt one is fully archived and recoverable:
- Branch **`archive/attempt-1`** / tag **`attempt-1`** — complete code + git history, CI green at archival (171/171 authorization tests passing)
- **`archive-dumps/`** on that branch — the live database at the moment of archival: full migration history (including the 27 live-only migrations the repo never had), every table's data, auth users, and storage inventory
- Planning docs, audits, and the attempt-one retrospective live in the Shadowdark Claude project
